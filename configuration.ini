# DO NOT EDIT DEFAULT SECTION
[DEFAULT]
feat_count = 10
gamma = 0 # tolerance parameter
delta = -1 # set >=0 for use of codependent multivariate 
get_bounds = False # set true to get boundaries in an output file
eval_flags = NA  # eval_bound / result / reason / eval_tool

# OVERWRITE IN USER SECTION IF YOU WANT TO CHANGE DEFAULT VALUES
[USER]
train = dev.csv
model = model.dat
test = loan_2019Q4.csv
; feat_imp = fscore.json
; bounds = bounds_file.json
; features = loan_amnt,out_prncp
pos_var = out_prncp
neg_var = na